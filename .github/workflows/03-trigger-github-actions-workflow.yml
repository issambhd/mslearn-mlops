name: GitHub Actions workflow triggered by the creation of a pull request

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Placeholder
        run: |
          echo "Will add code checks here in next challenge"
