$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
code: model
command: >-
  python train.py
  --training_data ${{inputs.training_data}}
  --reg_rate ${{inputs.reg_rate}}
inputs:
  training_data: 
    type: uri_folder 
    path: azureml:diabetes-dev-folder@latest
  reg_rate: 0.01
environment: azureml:AzureML-sklearn-0.24-ubuntu18.04-py37-cpu@latest
compute: amlcompute-mslearn-mlops
experiment_name: experiment-from-mslearn-mlops
description: | 
  # My Azure Ml job for the Challenge 3
  This is my mlops job Challenge 1 running in the cloud via Azure ML!
  ## Description 
  mslearn-mlops Challenge 3 Create an Azure Machine Learning job
